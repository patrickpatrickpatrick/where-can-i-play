import { createContext } from 'react';

export const SelectedGameContext = createContext({});
export const SelectedArcadeContext = createContext({});
export const ArcadesListContext = createContext([]);